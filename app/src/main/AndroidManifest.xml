<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Required for setting ringtones -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Storage -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:replace="android:maxSdkVersion"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Boot for auto-start -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- FeedX Permissions -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    
    <!-- VaultX Permissions -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".TorrentApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TorrentX"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:usesCleartextTraffic="true"
        android:extractNativeLibs="true"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="34">

        <!-- Disable default WorkManager initialization -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- WorkManager Foreground Service -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <meta-data
            android:name="android.webkit.WebView.MetricsOptOut"
            android:value="true"/>
        <meta-data
            android:name="android.max_aspect"
            android:value="2.1"/>

        <!-- Universal Home -->
        <activity
            android:name="com.codex.torrentx.UniversalHomeActivity"
            android:exported="true"
            android:theme="@style/Theme.TorrentX"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MusicX Advanced Audio Player -->
        <activity
            android:name="com.codex.torrentx.ui.player.MusicPlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.TorrentX"
            android:screenOrientation="portrait"
            android:launchMode="singleTask"
            android:taskAffinity="com.codex.torrentx.music"/>
            
        <activity
            android:name="com.codex.torrentx.ui.player.SpotifyAlbumScreenActivity"
            android:exported="false"
            android:theme="@style/Theme.TorrentX"
            android:screenOrientation="portrait"/>
            
        <!-- Advanced Media Grabber Dialog Host -->
        <activity
            android:name="com.codex.torrentx.ui.activity.MediaGrabberActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:screenOrientation="portrait"/>

        <!-- MusicX Library Activity -->
        <activity
            android:name="com.codex.torrentx.MusicXActivity"
            android:exported="false"
            android:theme="@style/Theme.TorrentX"
            android:screenOrientation="portrait"/>

        <activity
            android:name="com.codex.torrentx.TorrentDashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.TorrentX"
            android:screenOrientation="portrait">
            <!-- Handle magnet links -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="magnet" />
            </intent-filter>
            
            <!-- Handle .torrent files -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:mimeType="application/x-bittorrent" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="file" />
                <data android:mimeType="application/x-bittorrent" />
            </intent-filter>
        </activity>



        <!-- VideoX Library Activity -->
        <activity
            android:name="com.codex.torrentx.ui.library.VideoLibraryActivity"
            android:exported="false"
            android:theme="@style/Theme.TorrentX"
            android:screenOrientation="portrait"/>

        <activity
            android:name="com.codex.torrentx.VideoXActivity"
            android:exported="true"
            android:theme="@style/Theme.TorrentX"
            android:screenOrientation="portrait"/>

        <activity
            android:name="com.codex.torrentx.DownloadXActivity"
            android:exported="true"
            android:theme="@style/Theme.TorrentX"
            android:screenOrientation="portrait"/>

        <activity
            android:name="com.codex.torrentx.M3u8Activity"
            android:exported="true"
            android:theme="@style/Theme.TorrentX"
            android:screenOrientation="portrait"/>


        <!-- BrowserPlayerActivity Removed -->

        <!-- Legacy Browser 2026 Activity (Removed) -->
        <!-- <activity android:name=".NeoBrowserActivity" ... /> -->

        <!-- Legacy MainActivity removed -->

        <activity
            android:name="com.browserx.app.MainActivity"
            android:exported="true"
            tools:replace="android:theme,android:configChanges"
            android:theme="@style/Theme.TorrentX"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <!-- Legacy Browser Activity Removed -->

        <activity
            android:name=".ui.player.PlayerActivity"
            android:exported="true"
            android:theme="@style/Theme.TorrentX"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize"
            android:supportsPictureInPicture="true">
            
            <!-- Deep Link -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="torrentx" android:host="play" />
            </intent-filter>

            <!-- File / Content (Video) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="video/*" />
            </intent-filter>

            <!-- Network Streams -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:mimeType="video/*" />
                <data android:mimeType="application/x-mpegURL" />
                <data android:mimeType="application/dash+xml" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.PlaybackService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService"/>
            </intent-filter>
        </service>






        <!-- Torrent Download Service -->
        <service
            android:name=".service.TorrentService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <service
            android:name=".service.UniversalDownloadService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".service.aria2.Aria2Service"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".service.HlsRecordingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Download Action Receiver for Quick Actions -->
        <receiver
            android:name=".service.DownloadActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        
        <!-- FeedX Reminder Receivers -->
        <receiver
            android:name=".service.ReminderReceiver"
            android:enabled="true"
            android:exported="false" />
            
        <receiver
            android:name=".service.ReminderActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"
                tools:replace="android:resource" />
        </provider>

        <!-- Google Cast Options Provider -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.codex.torrentx.cast.CastOptionsProvider" />


    </application>

</manifest>
